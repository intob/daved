[Unit]
Description=daved
After=network.target

[Service]
Type=simple
ExecStart=%h/daved -v -f -e aws1:127 -backup backup.dave
WorkingDirectory=%h
Restart=on-failure

[Install]
WantedBy=multi-user.target
