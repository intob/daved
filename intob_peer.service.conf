[Unit]
Description=daved
After=network.target

[Service]
Type=simple
ExecStart=%h/daved -v -backup backup.dave -e edge1:127
WorkingDirectory=%h
Restart=on-failure

[Install]
WantedBy=multi-user.target
